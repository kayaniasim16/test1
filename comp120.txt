git config --global user.name "kayaniasim16"
git config --global user.email "kayaniasim16@gmail.com"
mkdir test1
cd test1
git init
git remote add origin https://github.com/kayaniasim16/test1.git
"Hello" | Out-File -FilePath comp120.txt
"How" | Out-File -Append -FilePath comp120.txt
"Are you" | Out-File -Append -FilePath comp120.txt
git add comp120.txt
git commit -m "Add comp120.txt with three lines"
git branch -M main
git push -u origin main
$lines = Get-Content comp120.txt
$lines[0] = "Hi"
$lines | Set-Content comp120.txt
git add comp120.txt
git commit -m "Change first line from Hello to Hi"
git push
git log --oneline

